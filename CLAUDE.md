@AGENTS.md
@AGENTS.policy.md
# Project: statistics-for-data-science

## Git Branch Policy

이 repo는 `AGENTS.policy.md`의 Branch and merge workflow를 따른다. `main`에서 별도 worktree branch를 만들고, PR을 `main`으로 열어 squash merge한다. `main` 직접 push와 `dev` integration branch는 사용하지 않는다.
